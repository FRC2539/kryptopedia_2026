name: kryptopedia
description: "A new Flutter project."
publish_to: "none"
version: 2026.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  confetti: ^0.8.0
  package_info_plus: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
