# kryptopedia

a beautiful little flutter app :)

## Development setup

pretty simple. clone the repo, and open this directory in VS Code. Make sure you have Flutter installed and the Flutter extension for VS Code. Run `flutter pub get` in the terminal, then open up a file in `lib` and press F5.
